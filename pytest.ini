[pytest]
# Default collection is the OFFLINE unit suite (pure functions + mocked async
# pipeline) so a bare `pytest` never spends API budget. The integration tests
# under tests/integration hit the LIVE Lakera API (conftest loads .env), so run
# them explicitly: `pytest tests/integration`.
asyncio_mode = auto
testpaths = tests/unit
python_files = test_*.py
