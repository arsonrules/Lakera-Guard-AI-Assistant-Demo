# Secrets — never commit real credentials
.env

# Python build artifacts
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/

# Virtual environments
.venv/
venv/
env/

# IDE and local tool settings
.claude/
.idea/
.vscode/
*.DS_Store

# User-uploaded custom RAG documents (runtime state, not part of the repo)
tests/fixtures/docs_custom/*.txt

# Saved one-shot run history (regression baselines — local/CI state)
runs/

# Generated one-shot reports (CLI --output-dir artifacts)
reports/

# Downloaded HuggingFace dataset cache (CLI --hf-download)
datasets/
config/
